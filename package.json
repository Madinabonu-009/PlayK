{
  "name": "play-kids-kindergarten",
  "version": "1.0.0",
  "description": "Play Kids - Modern Kindergarten Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run install:all && npm run build:frontend",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "render-build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "prepare": "husky install",
    "lint": "npm run lint --prefix frontend",
    "lint:fix": "npm run lint:fix --prefix frontend",
    "db:indexes": "node backend/src/scripts/createIndexes.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/play-kids.git"
  },
  "keywords": [
    "kindergarten",
    "education",
    "management",
    "react",
    "nodejs"
  ],
  "author": "Play Kids Team",
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "frontend/src/**/*.{js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "backend/src/**/*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "**/*.{json,md,css}": [
      "prettier --write"
    ]
  }
}
